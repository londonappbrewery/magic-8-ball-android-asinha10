<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/holo_blue_light"
    android:paddingBottom="16dp"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="16dp"
    android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/view_topic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:text="@string/title"
        android:textColor="@android:color/background_dark"
        android:textSize="24sp"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/image_eightBall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/askButton"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="37dp"
        android:contentDescription="EightBall Image"
        android:src="@drawable/ball1" />

    <Button
        android:id="@+id/askButton"
        style="@style/Widget.AppCompat.Button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/view_topic"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/view_topic"
        android:layout_marginBottom="22dp"
        android:background="@color/colorPrimaryDark"
        android:text="@string/askButton"
        android:textColor="?android:attr/colorBackground"
        android:textSize="24sp"
        android:textStyle="bold" />
</RelativeLayout>
